---
import ExternalLink from "@lucide/astro/icons/external-link";
import EventActions from "./EventActions.svelte";
import Badge from "../ui/badge.svelte";
import Table from "../ui/table.svelte";
import TableBody from "../ui/table-body.svelte";
import TableCell from "../ui/table-cell.svelte";
import TableHead from "../ui/table-head.svelte";
import TableHeader from "../ui/table-header.svelte";
import TableRow from "../ui/table-row.svelte";
import type { EventRecord } from "../../lib/db/queries";
import { getDivisionLabel } from "../../lib/divisions";
import { formatEventSchedule } from "../../lib/events/datetime";
import { getEventScaleLabel, getEventTypeLabel } from "../../lib/events/options";

interface Props {
    events: EventRecord[];
    mode: "pending" | "published" | "offline";
}

const { events, mode } = Astro.props;

const statusLabels: Record<string, string> = {
    pending: "待审核",
    published: "已发布",
    rejected: "已驳回",
    offline: "已下线"
};
const statusTones: Record<string, "default" | "accent" | "warning" | "danger"> = {
    pending: "warning",
    published: "accent",
    rejected: "danger",
    offline: "default"
};

const mobileCell =
    "min-w-0 before:mb-1 before:block before:text-[0.6875rem] before:font-semibold before:text-muted before:content-[attr(data-label)] lg:before:hidden";
---

{
    events.length === 0 ? (
        <div class="border-border bg-surface text-muted-foreground flex min-h-44 items-center justify-center rounded-md border px-6 text-center text-sm">
            当前队列暂无活动。
        </div>
    ) : (
        <div class="lg:border-border lg:bg-surface lg:overflow-x-auto lg:rounded-md lg:border">
            <Table class="text-foreground block w-full text-sm lg:table lg:min-w-[70rem]">
                <caption class="sr-only">
                    {mode === "pending"
                        ? "待审核活动"
                        : mode === "published"
                          ? "已发布活动"
                          : "已下线活动"}
                </caption>
                <TableHeader class="sr-only lg:not-sr-only lg:table-header-group">
                    <TableRow class="border-border border-b hover:bg-transparent">
                        <TableHead class="text-muted-foreground h-auto w-[18%] px-4 py-3 text-left text-xs font-semibold">
                            活动
                        </TableHead>
                        <TableHead class="text-muted-foreground h-auto w-[18%] px-4 py-3 text-left text-xs font-semibold">
                            时间地点
                        </TableHead>
                        <TableHead class="text-muted-foreground h-auto w-[13%] px-4 py-3 text-left text-xs font-semibold">
                            分类
                        </TableHead>
                        <TableHead class="text-muted-foreground h-auto w-[17%] px-4 py-3 text-left text-xs font-semibold">
                            来源与联系
                        </TableHead>
                        <TableHead class="text-muted-foreground h-auto px-4 py-3 text-left text-xs font-semibold">
                            说明
                        </TableHead>
                        <TableHead class="text-muted-foreground h-auto w-36 px-4 py-3 text-left text-xs font-semibold">
                            操作
                        </TableHead>
                    </TableRow>
                </TableHeader>
                <TableBody class="grid gap-3 lg:table-row-group [&_tr:last-child]:border lg:[&_tr:last-child]:border-0">
                    {events.map((event) => (
                        <TableRow class="border-border bg-surface hover:bg-surface lg:hover:bg-surface-subtle/60 grid grid-cols-2 gap-x-4 gap-y-4 rounded-md border p-4 lg:table-row lg:rounded-none lg:border-x-0 lg:border-t-0 lg:p-0">
                            <TableCell class="border-border col-span-2 border-b p-0 pb-3 align-top lg:table-cell lg:border-b-0 lg:p-4">
                                <div class="flex items-start justify-between gap-3 lg:block">
                                    <div class="min-w-0">
                                        <div class="text-foreground leading-5 font-bold break-words">
                                            {event.title}
                                        </div>
                                        <div class="text-muted mt-1 text-xs">活动 #{event.id}</div>
                                    </div>
                                    <Badge
                                        tone={statusTones[event.status] ?? "default"}
                                        class="shrink-0"
                                    >
                                        {statusLabels[event.status] ?? event.status}
                                    </Badge>
                                </div>
                            </TableCell>
                            <TableCell
                                class:list={[mobileCell, "align-top lg:table-cell lg:p-4"]}
                                dataLabel="时间与地点"
                            >
                                <div class="text-foreground font-semibold">
                                    {formatEventSchedule(event)}
                                </div>
                                <div class="text-muted-foreground mt-1">
                                    {getDivisionLabel(event.division_code) ?? "未设置地区"}
                                </div>
                                <div class="text-muted mt-0.5 text-xs">{event.venue}</div>
                                {event.address ? (
                                    <div class="text-muted mt-0.5 text-xs">{event.address}</div>
                                ) : null}
                            </TableCell>
                            <TableCell
                                class:list={[mobileCell, "align-top lg:table-cell lg:p-4"]}
                                dataLabel="分类"
                            >
                                <div class="text-foreground font-semibold">
                                    {getEventTypeLabel(event.type)}
                                </div>
                                <div class="text-muted-foreground mt-1">
                                    {getEventScaleLabel(event.scale)}
                                </div>
                                {event.tags ? (
                                    <div class="text-muted mt-1 text-xs break-words">
                                        {event.tags}
                                    </div>
                                ) : event.tag_suggestions ? (
                                    <div class="text-warning mt-1 text-xs break-words">
                                        投稿建议：{event.tag_suggestions}
                                    </div>
                                ) : null}
                            </TableCell>
                            <TableCell
                                class:list={[
                                    mobileCell,
                                    "col-span-2 align-top sm:col-span-1 lg:table-cell lg:p-4"
                                ]}
                                dataLabel="来源与联系"
                            >
                                <div class="flex flex-wrap gap-x-3 gap-y-1">
                                    <a
                                        href={event.source_url}
                                        target="_blank"
                                        rel="nofollow noopener noreferrer"
                                        class="text-link focus-visible:ring-ring/60 inline-flex items-center gap-1 font-semibold underline-offset-2 hover:underline focus-visible:ring-2 focus-visible:outline-none"
                                    >
                                        来源
                                        <ExternalLink class="size-3" aria-hidden="true" />
                                    </a>
                                    {event.ticket_url ? (
                                        <a
                                            href={event.ticket_url}
                                            target="_blank"
                                            rel="nofollow noopener noreferrer"
                                            class="text-link focus-visible:ring-ring/60 inline-flex items-center gap-1 font-semibold underline-offset-2 hover:underline focus-visible:ring-2 focus-visible:outline-none"
                                        >
                                            购票
                                            <ExternalLink class="size-3" aria-hidden="true" />
                                        </a>
                                    ) : null}
                                    {event.cover_url ? (
                                        <a
                                            href={event.cover_url}
                                            target="_blank"
                                            rel="nofollow noopener noreferrer"
                                            class="text-link focus-visible:ring-ring/60 inline-flex items-center gap-1 font-semibold underline-offset-2 hover:underline focus-visible:ring-2 focus-visible:outline-none"
                                        >
                                            封面
                                            <ExternalLink class="size-3" aria-hidden="true" />
                                        </a>
                                    ) : null}
                                </div>
                                <div class="text-muted mt-2 text-xs break-all">
                                    {event.submitter_contact}
                                </div>
                            </TableCell>
                            <TableCell
                                class:list={[
                                    mobileCell,
                                    "col-span-2 align-top sm:col-span-1 lg:table-cell lg:max-w-xs lg:p-4"
                                ]}
                                dataLabel="补充说明"
                            >
                                <div class="text-muted-foreground line-clamp-3 leading-5">
                                    {event.description ?? "无活动说明"}
                                </div>
                                {event.qq_group ? (
                                    <div class="text-muted mt-1 text-xs">
                                        QQ群：{event.qq_group}
                                    </div>
                                ) : null}
                            </TableCell>
                            <TableCell
                                class:list={[
                                    mobileCell,
                                    "border-border col-span-2 border-t pt-3 align-top lg:table-cell lg:border-t-0 lg:p-4"
                                ]}
                                dataLabel="操作"
                            >
                                <EventActions
                                    eventId={event.id}
                                    {mode}
                                    hasTags={Boolean(event.tags)}
                                    client:load
                                />
                            </TableCell>
                        </TableRow>
                    ))}
                </TableBody>
            </Table>
        </div>
    )
}
